# 应用报告文件夹

- `创AI-开发与应用报告-终稿.docx`：最终交付文件
- `图资源/`：已放入报告中的终稿图片。当前仅保留报告实际使用的项目图片与结构图。
- `中间过程/`：保留仍有生成价值的脚本、预览文件和历史渲染产物

整理说明：

- 与案例包根目录主文稿完全重复的 `.md` 副本已移入 `../archive/duplicate-manuscripts/report-intermediate/`。
- 与案例包根目录脚本完全重复的脚本副本已移入 `../archive/duplicate-generators/report-intermediate/`。
- 与交付区重复的历史 `docx` 副本已移入 `../archive/duplicate-docx/report-intermediate/`。
- 这里现在主要保留报告专属预览、图资源和仍在使用的过程脚本。

## 当前图资源口径

- 报告与 PPT 共用同一套核心图资源，避免两份材料叙事不一致。
- 图资源覆盖：课堂背景、系统框架、学习路径、真实课程页、课程包结构、Blockly 工作流、Notebook/Python 代码、样例运行结果、AI 辅助开发和应用价值。
- 未采用或表达不清的 AI 生成图已归档到 `中间过程/未采用图资源-20260528/`，不再出现在终稿图资源目录。

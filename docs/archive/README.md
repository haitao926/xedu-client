# 文档归档区

本目录用于存放已经不适合继续占据仓库主路径、但仍需要保留以便回溯的历史文档。

当前归档策略：

- `root-history/`：仓库根目录迁移下来的历史说明文档。
- 仅归档历史说明、重复记录、过时操作手册，不放当前主入口文档。
- 需要继续引用或维护的文档，应保留在其原有业务目录中。

使用原则：

- 优先查业务目录中的 `README.md` 和当前主文稿。
- 只有在需要回看历史过程或旧版本说明时，再进入本目录。

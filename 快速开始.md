# Jupyter Tauri - 快速开始指南

## 🎯 一键打包 (推荐)

运行自动化打包脚本:

```bash
python package_for_distribution.py
```

此脚本将自动:
1. ✅ 构建Tauri应用
2. ✅ 创建完整部署包
3. ✅ 生成安装脚本

**输出**: `jupyter-tauri-distribution/` 目录

## 📦 手动打包 (备选)

如果自动脚本失败，请按以下步骤操作:

### 1. 构建Tauri
```bash
npm run tauri:build
```

### 2. 创建部署目录
```bash
mkdir jupyter-tauri-deploy
cd jupyter-tauri-deploy
```

### 3. 复制必要文件
```bash
# 复制Tauri应用
copy "..\src-tauri\target\release\Jupyter-Lab-Client.exe" .

# 复制启动器
copy "..\launch_jupyter.bat" .

# 复制资源目录
xcopy "..\src-tauri\resources" /E /I
```

### 4. 部署包结构
```
jupyter-tauri-deploy/
├── Jupyter-Lab-Client.exe       # Tauri应用
├── launch_jupyter.bat            # 启动器
└── resources/                    # Python环境
    ├── venv/
    └── web_app.py
```

## 🚀 使用部署包

### Windows用户
1. 双击 `launch_jupyter.bat`
2. 等待启动
3. 访问 http://127.0.0.1:8888/lab

### Linux/Mac用户
```bash
chmod +x launch_jupyter.sh
./launch_jupyter.sh
```

## 📋 项目完成情况

### ✅ 已完成
- [x] 创建Python 3.13虚拟环境
- [x] 安装Flask + JupyterLab
- [x] 移除硬编码路径
- [x] 使用Jupyter Lab (非Notebook)
- [x] 进程保护功能
- [x] 清理项目文件
- [x] 集成到Tauri
- [x] 创建启动器脚本
- [x] 打包工具
- [x] 完整文档

### 📊 技术栈
- **前端**: HTML + JavaScript (Tauri WebView)
- **后端**: Rust (Tauri)
- **API**: Python 3.13 + Flask
- **Jupyter**: JupyterLab 4.4.10
- **桌面框架**: Tauri 2.x
- **打包**: 跨平台 (Windows/macOS/Linux)

## 📁 关键文件说明

| 文件 | 用途 |
|------|------|
| `package_for_distribution.py` | 自动化打包脚本 |
| `launch_jupyter.bat` | Windows启动器 |
| `TAURI_PACKAGE_SUMMARY.md` | 完整打包指南 |
| `src-tauri/resources/` | Python环境(打包时包含) |
| `config.json` | 配置文件 |

## 🔧 开发命令

```bash
# 开发模式运行
npm run tauri:dev

# 构建Tauri应用
npm run tauri:build

# 运行Flask API (开发用)
venv\Scripts\python.exe web_app.py
```

## 📞 访问地址

- **Jupyter Lab**: http://127.0.0.1:8888/lab
- **Flask API**: http://127.0.0.1:5000
- **API状态**: http://127.0.0.1:5000/api/status

## ⚠️ 注意事项

1. **端口**: 确保5000和8888端口未被占用
2. **权限**: Linux/Mac需要执行权限: `chmod +x *.sh *.AppImage`
3. **防病毒**: 可能需要将Python.exe加入白名单
4. **系统**: Windows 10+, macOS 10.15+, Ubuntu 18.04+

## 🎉 完成!

项目已准备就绪，可进行打包和分发。

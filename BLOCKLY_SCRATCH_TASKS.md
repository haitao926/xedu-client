# Blockly / Scratch Task List

_Updated: 2026-07-09_

## 背景

当前项目已经完成一轮重要收敛：课程内容生产能力已经从 App 主界面迁移到 Claude skills，客户端本身正在收敛为“运行、展示、分发、消费课程”的终端。

在这个基础上，可视化编程部分出现了一个新的决策点：

1. **当前 Blockly 界面本身还有明显的视觉和教学体验问题，需要先优化**
2. **从更长远的方向看，Scratch 可能更适合作为未来“模型控制角色与舞台表达”的主表达层**

所以接下来的工作不能只看“积木好不好看”，而要同时处理两个层次的问题：

- **短期问题**：怎么把现在的 Blockly 做得更清楚、更顺手、更适合课堂
- **中长期问题**：未来如果模型要控制角色、舞台、动作和反馈，Scratch 是否更适合成为上层表达系统

这就是为什么这里需要拆成两条独立任务线，而不是把所有想法混在一个 PR 里。

---

## 总目标

### 近期目标

把当前 Blockly 界面从“功能可用但视觉噪声较大、信息组织不够清楚”提升到：

- 课堂中老师愿意用
- 学生一眼能看懂分类与入口
- 积木本体更容易读
- 整体更稳定、可信、专业，而不是特效堆叠

### 中期目标

明确 Scratch 是否应该成为未来的主表达平台，并把这个判断建立在一个可验证的技术路径上，而不是停留在“看起来更好”或“感觉上更合适”。

### 长期目标

为未来的能力打基础：

> 训练一个模型，在可视化舞台里控制角色、动作、状态、台词与反馈，形成更生动、更形象的表达系统。

如果未来真正要做到这一点，那么 Scratch 方向就不只是“换一种积木样式”，而是**建立新的表达语义层**。

---

## 为什么要拆成两条线

## 线 A：先优化当前 Blockly UI

### 为什么要做

即使未来真的转向 Scratch，当前版本依然需要可用。

原因很实际：

1. **现在的 Blockly 已经承载了真实功能**
   - 不是一个演示页面，而是已经接了 runtime、task registry、积木定义和课程体验
   - 直接放着不管，会让当前版本继续显得“能用但不顺眼、不清楚”

2. **现在的“丑/乱”主要来自主题层和视觉收敛问题，不是底层引擎完全不行**
   - 如果不先把这件事拆清楚，就很容易误判成“只有换 Scratch 才能解决”

3. **优化当前 UI 也能反过来帮助判断未来是否真的要换**
   - 如果视觉收敛后体验明显变好，说明当前问题大部分在主题层
   - 如果收敛后仍然不适合课堂表达，才更能证明 Scratch 路线的必要性

4. **这是当前可交付版本的质量问题**
   - 就算未来另起 Scratch 分支，主线产品也不能长时间保持在“视觉和层级明显不理想”的状态

### 这条线的核心判断

这条线解决的是：

> “当前版本如何变得更清楚、更好教、更稳定。”

而不是：

> “未来是否要完全切到 Scratch。”

---

## 线 B：探索 Scratch 分支

### 为什么要做

你们现在想要的未来，不只是“拖积木写逻辑”，而是：

- 模型可以控制角色
- 角色可以在舞台里行动
- 表达是动态的、具象的、叙事化的
- 学生看到的是舞台/角色/反馈，而不是纯流程图式的任务调用

这时候，Scratch 的优势不是“更可爱”，而是它本身就以这些对象为中心：

- stage
- sprite
- costume
- motion
- looks
- event
- broadcast
- variable/list

也就是说，如果未来的主问题变成：

> “如何让模型以可视化角色的方式表达和教学”

那么 Scratch 更像是一种产品语言，而不是单纯一个编辑器皮肤。

### 为什么不能和当前 Blockly 优化混在一起

因为两者解决的是不同层级的问题：

- **Blockly polish** 是当前产品质量改进
- **Scratch spike** 是未来表达层架构验证

如果混在一起，会出现 4 个问题：

1. PR 太大，难 review
2. 很难判断问题来源
3. 当前产品质量问题和未来架构探索互相干扰
4. 团队会陷入“到底是在修 UI，还是在换平台”的混乱状态

所以必须分开。

---

## 工作原则

- **不把 Blockly UI polish 和 Scratch 探索混在一个 PR 里**
- **先解决当前可用性，再探索未来路线**
- **Scratch 方向的目标不是简单换皮，而是为“舞台 + 角色 + 模型控制”建立表达层**
- **底层 AI / runtime 能力可以保留在现有服务层，通过 bridge / extension 接入 Scratch**
- **当前优化只改现有产品体验，不顺手引入架构级变更**
- **Scratch 分支优先做最小可验证原型，而不是一上来替换全部 Blockly 能力**

---

# Task A — 当前 Blockly UI 优化

## A1. 盘点当前 UI 问题

### 为什么做

在改之前，必须先搞清楚“到底哪里丑、为什么丑”。

如果不先做这个动作，很容易：

- 一边删阴影一边加新装饰，最后更乱
- 把局部问题误判成整体架构问题
- 只能凭感觉修改，无法形成稳定风格

这个任务的价值是建立一份**有结构的问题地图**。

### 怎么做

重点从 4 个维度检查当前 Blockly 体验：

1. **toolbox / sidebar**
   - 分类是否太多层
   - 分类是否难扫读
   - 图标是否喧宾夺主
   - 选中态是否过重

2. **block 本体**
   - 文字是否容易读
   - 块颜色是否稳定且可区分
   - 阴影/描边/高光是否太多
   - 字段、下拉、输入框是否显得过度修饰

3. **icon 系统**
   - 是否存在过多不同风格图标混用
   - 图标是否真的帮助理解，还是只是在增加复杂度
   - 哪些图标必须保留，哪些应该降级或删除

4. **workspace chrome**
   - topbar、panel、flyout、输出区之间是否都在抢注意力
   - 哪个区域应该是主角，哪个区域应该退后
   - 是否有“控制台感”太强的问题

### 重点文件

- `renderer/styles/blockly-workspace.css`
- `renderer/js/blockly/runtime-appearance.js`
- `renderer/js/blockly/xeduhub-blocks.js`

### 输出

一份问题清单，按下面结构整理：

- 问题描述
- 为什么这是问题
- 影响的是谁（老师 / 学生 / 课堂演示）
- 优先级（高 / 中 / 低）

---

## A2. 产出 UI 收敛方案

### 为什么做

盘点问题之后，不能直接“想到哪改到哪”，而是要先确定一个明确的目标风格。

否则就会出现这种情况：

- 某些区域被改得更简洁
- 某些区域又继续保留强装饰
- 整体仍然像多个设计方向拼接在一起

这个任务的意义是：

> 把“不要那么丑”变成“应该长成什么样”。

### 怎么做

这一步要把目标说清楚，而不是只写“更好看”。

需要明确回答：

1. **当前 Blockly 的目标气质是什么**
   - 更课堂化
   - 更平静
   - 更清楚
   - 更偏“教育控制台”还是更偏“儿童创作工具”

2. **哪一个区域是视觉主角**
   - 我建议是 block 本体和工作区，而不是 sidebar 或 topbar

3. **哪些元素必须降级**
   - 例如：过重阴影、过多 3D icon、复杂发光、过多选中强调

4. **哪些元素需要保留作为识别骨架**
   - 稳定的类别色
   - 清晰的分组结构
   - 适度但统一的图标系统

### 需要回答的问题

- 是否弱化或去掉 3D category icon
- toolbox 是否改成更直接的分组方式
- block 是否改得更平、更粗、更好读
- 哪些高光/阴影/描边应该删掉
- 哪些颜色应该保留，哪些应该收束
- 选中态是否从“装饰强调”改成“结构强调”

### 输出

一份可直接实施的 UI 优化清单，形式建议是：

- 保留什么
- 删除什么
- 弱化什么
- 替换成什么
- 为什么这么做

---

## A3. 实施当前 Blockly UI 优化

### 为什么做

A1 和 A2 都只是分析与方案，真正改善当前体验必须落到代码上。

这一轮实施的重点不是“做一个新产品”，而是：

- 用最少的结构风险
- 换来最明显的体验提升

### 怎么做

优先从 CSS 层入手，因为当前问题大概率主要在视觉层：

#### 优先修改

- `renderer/styles/blockly-workspace.css`

优先改它的原因是：

- 风险最低
- 见效最快
- 最适合先做“去噪、收敛、统一”

#### 只有在必要时才动 JS 配套

- `renderer/js/blockly/runtime-appearance.js`
- `renderer/js/blockly/xeduhub-blocks.js`

这两处只在以下情况才建议动：

- 需要减少图标层级
- 需要统一 category icon 生成策略
- 需要清理积木内的装饰字段或不必要视觉元素

### 实施时建议优先级

1. 先收拾 sidebar/toolbox
2. 再收拾 block 本体
3. 再统一 flyout / panel / output chrome
4. 最后决定是否减图标系统复杂度

### 非目标

- 不引入 Scratch runtime
- 不在这一轮改底层任务能力
- 不动课程结构与资源系统主逻辑
- 不做架构迁移
- 不试图一次性解决“未来表达层”的问题

### 输出

当前版本 Blockly UI 的可用优化结果，要求：

- 风格比之前更统一
- 学生更容易扫读
- 老师更容易课堂演示
- 视觉噪声比之前明显下降

---

## A4. 验证当前 Blockly UI

### 为什么做

只改样式也有可能破坏功能，比如：

- 工具箱可点击区域错位
- flyout 显示异常
- 文本被裁剪
- block 字段无法编辑
- 运行时布局在不同窗口宽度下崩掉

所以验证不能省。

### 怎么做

建议至少做下面几类验证：

#### 语法与测试

- `npm run check:renderer-syntax`
- `npm run test:blockly-runtime`

#### 必要时的构建验证

- `npm run build`

#### 必要时的运行时 smoke test

- 打开 Blockly 页面
- 看 sidebar / workspace / flyout 是否正常
- 拖几个积木
- 点击字段 / 下拉
- 跑一次最基本的示例

### 输出

- 验证结果
- 如果失败，记录失败点
- 如果通过，记录还有哪些视觉细节以后再做

---

# Task B — Scratch 分支探索

## B1. 定义 Scratch 分支的目标

### 为什么做

如果不先定义目标，很容易把 Scratch 分支做成一个“既不像正式迁移，也不像验证原型”的东西，最后没有结论。

所以这一任务的核心是：

> 先写清楚这条分支到底是在验证什么。

### 怎么做

要明确它不是：

- 马上替换整个 Blockly
- 一次性迁走所有 AI 能力
- 追求功能齐平

而是验证：

> Scratch 是否更适合作为未来“模型控制舞台角色与表达”的主表达层。

### 输出

一个简短 branch brief，至少写清：

- 这条分支要证明什么
- 不证明什么
- 为什么现在做这个验证
- 成功标准是什么
- 如果失败，失败意味着什么

---

## B2. 做能力映射：Blockly → Scratch

### 为什么做

这个任务是防止团队掉进一个常见误区：

> 以为“Scratch 更适合表达”就等于“Scratch 能直接替代当前所有 Blockly 能力”。

实际上两者不是一回事。

所以必须先做能力拆分。

### 怎么做

把当前系统能力拆成两层：

## 可由 Scratch 原生承接的表达层

这些是 Scratch 很自然能做的：

- 舞台
- 角色
- 动作
- 造型
- 事件
- 广播
- 变量 / 列表
- 交互反馈
- 剧情 / 演示式流程

## 需要 bridge / extension 才能承接的能力层

这些不是 Scratch 白送的：

- AI 任务调用
- 图像识别 / OCR / 姿态 / 摄像头
- 本地 runtime
- Python 生成
- 其他工程能力

### 这一步真正要回答的问题

- 当前哪些 Blockly 编排模式可直接迁为 Scratch 编排
- 哪些 XEdu AI 能力应该保留在服务层
- 哪些能力需要做 Scratch extension
- 未来是单轨还是双轨：
  - Scratch 做表达
  - Blockly/XEdu 保留部分工程能力

### 输出

一张映射表，至少包含：

- 当前能力名称
- 属于表达层还是能力层
- Scratch 是否原生支持
- 是否需要 extension / bridge
- 推荐迁移策略

---

## B3. 设计 Scratch 最小原型

### 为什么做

方向判断不能只靠讨论，最终还是要用一个最小可运行原型来验证。

但这个原型不能过大，否则会变成一个“新项目”，验证成本太高。

所以要先定义最小范围。

### 怎么做

我建议原型只验证最关键的未来价值：

> 模型或 AI 能力能不能通过 Scratch 的舞台/角色体系，形成更生动的表达。

### 建议最小范围

- 1 个 stage
- 1~2 个 sprite
- 基础能力：
  - move
  - say
  - switch costume
- 一个事件 / broadcast 机制
- 一个 AI 能力桥接点
  - 例如：识别结果驱动角色台词或动作

### 为什么这个范围足够

因为这已经能证明最关键的一件事：

> Scratch 是否真的比当前 Blockly 更适合承载“角色化表达”。

### 输出

- 最小实现范围
- 技术草图
- 关键依赖
- 明确哪些东西暂时不做

---

## B4. 准备 Scratch 分支 / PR 计划

### 为什么做

只有做成具体的 GitHub 分支 / PR 计划，团队才知道怎么分工、怎么 review、怎么判断是否继续。

否则 Scratch 讨论永远只会停留在方向层。

### 怎么做

需要先约束这条 PR：

- 是 spike，不是产品正式迁移
- 是验证表达层，不是全量能力替换
- 是单独分支，不侵入当前 Blockly polish 主线

### 建议 branch 名

- `feat/scratch-stage-spike`
- 或 `explore/scratch-runtime-bridge`

### PR 应包含

- 目标
- 为什么现在做
- 最小功能范围
- 明确非目标
- 风险点
- 验收标准
- 后续可能分叉出的正式任务

### 输出

一份可执行的 PR plan，可以直接拿去开 GitHub 分支和写 PR 描述。

---

## 推荐顺序

1. A1. 盘点当前 UI 问题
2. A2. 产出 UI 收敛方案
3. A3. 实施当前 UI 优化
4. A4. 验证当前 UI
5. B1. 定义 Scratch 分支目标
6. B2. 做能力映射
7. B3. 设计 Scratch 最小原型
8. B4. 准备 Scratch 分支 / PR 计划

### 为什么这个顺序合理

因为它遵循的是：

- 先修当前版本质量
- 再做未来路线验证
- 先让现在可用
- 再决定未来怎么迁

这样团队不会因为未来方向而放弃当前产品质量，也不会因为当前修补而拖延未来验证。

---

## 交付物清单

## 当前版本交付物

- 一版更干净的 Blockly UI
- 一套清楚的视觉收敛原则
- 验证通过的当前实现

## 下一阶段交付物

- 一份 Scratch 路线验证说明
- 一张 Blockly → Scratch 能力映射表
- 一个 scratch spike 分支 / PR 计划
- 一个最小原型范围定义

---

## 风险与约束

### 风险

1. **当前 Blockly polish 做得太大**
   - 会浪费在未来可能退场的表达层上
   - 所以这轮优化要以“收敛”和“清晰”为主，不要做大重构

2. **Scratch 探索做得太大**
   - 会把 spike 做成正式迁移，拖慢判断速度
   - 所以要严控最小范围

3. **混 PR 风险**
   - 一旦把两条线混在一起，review 和决策成本都会暴涨

### 约束

- 不要覆盖现有已经完成的主线 refactor 结论
- 不要把 Scratch 方向的探索塞进当前 Blockly polish PR
- 不要 push / commit，除非明确要求
- 不要把“更好看”当作 Scratch 路线唯一理由；真正理由应始终是“更适合舞台化角色表达”
